import { StyleSheet, Text, View } from "react-native";
import React from "react";

const NoticeInfoScreen = () => {
  return (
    <View>
      <Text>NoticeInfoScreen</Text>
    </View>
  );
};

export default NoticeInfoScreen;

const styles = StyleSheet.create({});
